#请使用Notepad++编辑和保存此文件，编码为"以UTF-8 无 BOM 格式编码"，
#请勿使用windows系统自带的记事本编辑和保存。
#
#
[基本信息]
#**必填值，描述程序作用的简短描述，不要写得太长，不建议超过 64个字符，超过1千个字符，将无法保存
程序名称=云汇宝-Amazon平台订单处理采集程序
#
#**必填值，唯一标记程序的编号，一经确定不能更改，可以使用当前时间值，比如 20170313151740，超过 256个字符将无法保存
程序编号=20170719170002
#
#**必填值，程序作者的名字
程序作者=胡晓飞
#
#选填值，脚本的最后更新时间，建议格式 2017-07-17 17:00:00
修改时间=2017-07-17 17:00
#
#**程序开始运行时间，格式 mm:ss。即设置程序第一次开始运行的时间，精确到分钟
程序运行时间=10:30
#
#**必填值，每次程序的运行时间间隔，核对程序会根据该值核对脚本是否成功运行过，
#  设置值为 时间+单位 ，时间为数字，单位为 h|d|w|m （时|天|周|月）, 比如 6h 为每6小时一次，3h 为每3小时运行一次，1d 为每天运行一次
运行间隔=1d
#
#**必填值，程序文件的名称，比如 demo.py, demo.kjb, demo.exe，如果是linux命令请填写linux.ssh并配置ssh的commanda项
程序入口=Amazon.exe
#
#
[ssh]
#*如程序入口为linux.ssh，则需要配置此项，值为要通过ssh运行的linux命令
LINUXCMD=spark-submit --master yarn /home/impt2hive/sparkScript/customer_spark.py;
#
#
[邮件]
#选填值，邮件接收者用户列表，可选值，如果有多个使用;进行分隔
邮件接收者=
#
#选填值，默认为 {程序名称}{程序编号}{date}{time}运行状态:{result}
邮件主题=
#
#
[备注]
#readme template with calling linux command via ssh
#选填表示该项值可以为空，但不能删除该项
